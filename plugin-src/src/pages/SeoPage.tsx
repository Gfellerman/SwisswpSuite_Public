/**
 * SEO Page — Phase 3 Migration Complete
 * Agent: debugger + backend-specialist
 * Wired to: SeoManager (full live component)
 * Features: AI meta generation, sitemap management, llms.txt, bulk optimize, health audit, product/post/image tabs
 */
import React from 'react';
import SeoManager from '../components/SeoManager';

export const SeoPage: React.FC = () => {
    return <SeoManager />;
};

export default SeoPage;
