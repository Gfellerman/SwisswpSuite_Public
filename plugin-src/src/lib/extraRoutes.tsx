import type { RouteObject } from "react-router-dom";

/**
 * Extension point for routes contributed on top of the built-in ones in
 * `router.tsx`. This build ships no additional routes.
 */
export const EXTRA_ROUTES: RouteObject[] = [];
