/**
 * Free-edition stub for settingsApiTabLabel.ts (WP.org string census
 * closure, 2026-08-13, R5). Wired via vite.config.ts's resolve.alias,
 * active only when EDITION === 'free'. Matches the label the Free build's
 * "api" tab has always shown (the "Editions & AI" informational section,
 * EditionsAiInfo.tsx).
 */
export const SETTINGS_API_TAB_LABEL = "Editions & AI";
