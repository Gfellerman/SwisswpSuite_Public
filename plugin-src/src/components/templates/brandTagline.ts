/**
 * The small uppercase line under the "SWISSSUITE" wordmark in the sidebar
 * header (DashboardLayout). The product NAME "SwissSuite AI" is rendered
 * separately wherever it appears and is not affected by this module — this
 * is only the descriptive line underneath it, and it must describe what
 * this build actually does.
 */
export const BRAND_TAGLINE = "SECURITY & BACKUP SUITE";
