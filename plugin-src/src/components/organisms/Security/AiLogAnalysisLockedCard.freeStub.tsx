import React from "react";

/**
 * Free-edition stub for AiLogAnalysisLockedCard.tsx (WP.org string census
 * closure, 2026-08-13, R2a). Wired via vite.config.ts's resolve.alias,
 * active only when EDITION === 'free'. Renders nothing — this card is
 * unreachable in a genuine Free install at runtime anyway (gated
 * isProEditionBuild at its SecurityHub.tsx call site).
 */
export const AiLogAnalysisLockedCard: React.FC = () => null;
