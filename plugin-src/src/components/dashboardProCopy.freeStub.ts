/**
 * Free-edition stub for dashboardProCopy.ts (ARS Round C P1-21 / F-41,
 * 2026-08-23). Wired via vite.config.ts's resolve.alias, active only when
 * EDITION === 'free'.
 *
 * The Free edition needs no license key at all, so this build never tells
 * a user to "activate" one — it just points at the actions that populate
 * the dashboard.
 */
export const DASHBOARD_EMPTY_STATE_DESCRIPTION =
  "Run a security scan or SEO check to populate your dashboard with real data from your site.";
